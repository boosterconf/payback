{
  "name": "booster-payback-time",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build node_modules/@vercel/blob/dist/client.js --outfile=public/blob-client.js --minify",
    "check": "tsc --noEmit",
    "dev": "vercel env pull --yes && bun run build && DEV_USER=true bun --env-file=.env.local --hot src/server.ts"
  },
  "devDependencies": {
    "@tsconfig/bun": "^1.0.10",
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.3",
    "hono": "^4.12.10"
  }
}
